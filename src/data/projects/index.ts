import { crescentTaxFiling } from "./crescent-tax-filing";
import { dattaSevaSamiti } from "./datta-seva-samiti";
import { noblemens } from "./noblemens";

/**
 * Ordered collection of projects displayed across the website.
 *
 * Keep the order intentional — it determines the default
 * presentation order in ProjectsExplorer.
 */
export const projects = [noblemens, dattaSevaSamiti, crescentTaxFiling];
